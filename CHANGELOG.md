# 更新日志（Changelog）

本文件记录「书到了」（ShudaoLe）各版本的用户可感知变更。格式参照
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [1.5.0] · 2026-09-10

**最终版**：暗夜模式上线，健壮性全面加固——此后项目进入维护模式，不再新增功能，仅修复平台变更导致的不可用。

### 新增

- **暗夜模式**：顶栏 🌙/☀️ 一键切换，暖黑底 + 印章红主色延续品牌视觉；首次使用自动跟随系统深浅色偏好，手动切换后记住选择；弹窗、表格、滚动条等全部控件同步适配，页面加载无闪白
- **CSV 全量导出**：结果超过展示上限时，「导出 CSV」自动获取全量数据（此前静默只导出前 300 条）
- **界面细节一批**：回到顶部按钮、页脚（版本/项目链接/版权提醒）、令牌输入「显示/隐藏」切换、文件大小支持 GB 显示

### 修复

- **平台变更防御（本版重点）**：教材下载地址失效（404）时自动轮换全部镜像与备用地址再报错，并在资源疑似下架时给出核对指引；平台目录接口返回异常数据时给出「接口可能已改版」的明确提示，不再显示代码错误；本地目录缓存损坏时自动重新下载
- **令牌获取失败引导**：一键获取令牌的各类失败（浏览器异常/连接断开/超时）提示统一补充「手动粘贴 x-nd-auth」的操作指引；令牌捕获兼容平台更换 CDN 域名的情况
- **下载任务防卡死**：下载编排出现意外错误时任务状态正确复位，不再出现「任务正在运行中」但实际无任务、只能重启程序的情况
- **CLI 筛选参数修复**：`--audience/--system/--school/--volume` 等参数单独使用时不再被忽略；`--school 普通学校` 自动映射为「常规学校」

### 变更

- **界面打磨一批**：弹窗支持 ESC 关闭并加入过渡动画；长列表表头吸顶；文字对比度提升至无障碍标准；表单基线对齐修正；下载进行中任务表不再闪烁、日志可自由上翻；圆角与间距体系统一；更新弹窗按钮布局与确认弹窗一致
- **文档**：README 增加维护模式声明与「已知限制」章节；开发路线图标记项目完结

### 升级

应用内「一键更新」可直接升级；或从下方附件下载。SHA256 见 SHA256SUMS.txt。

## [1.4.2] · 2026-09-10

「筛选顺手」版本：常用组合点一下就换过去，版本按热门程度排，高中不再出现没用的年级下拉。

### 修复

- **常用组合点了不生效**：点常用组合芯片（如「小学·人教全套」）后，旧年级/科目/关键词没清掉，组合条件与旧条件取交集常为 0 条；版本赋值靠 600ms 定时器猜测，选项还没渲染出来就被丢弃。现改为替换式应用：点组合芯片先清空全部筛选条件再设学段和版本，一次请求完成，不再有竞态
- **高中年级下拉误导**：高中教材 516 条中 497 条没有年级标签（平台用「必修/选择性必修」标注进度），下拉里只有「全部/一年级/二年级」三个选项，选一年级后主科全部消失，必须选「全部」才能找回。现根据年级标签覆盖率数据驱动：覆盖率不足 50% 时自动隐藏年级下拉，科目始终完整

### 变更

- **版本下拉按使用人数排序**：此前人教系列/北师大系列固定在最前面，不管当前学段谁用得多。现全部按命中数从上往下排，最常用的排最上；整套选项（如「人教版系（整套）」）的计数为组内成员之和，与真实标签混排

## [1.4.1] · 2026-09-10

「教师用书与下载清单」版本：教师教学用书获得独立视图入口，无效筛选组合从入口防呆，跨视图攒的下载清单首次看得见、对得上。

### 新增

- **「教师用书」视图**：教材视图栏新增「学生用书 / 教师用书」二态切换（默认学生用书，与六三/五四同款交互）。此前 550 条教师教学用书混在学生教材列表里、只在下载确认时提示一句——现在教师点一下即可独立浏览教参教辅，学段/年级/科目等候选随用书视图自动级联重算，选择本地记忆。默认视图不再混入教师用书，家长不会误下教师版
- **下载清单徽标与构成摘要**：结果栏新增「清单 N 条」徽标，点击跳到下载任务卡片并高亮输入框；卡片顶部显示清单构成（如「六三 2 · 五四 8 · 含教师用书 3」）。清单是跨视图累积的购物车，切换学制/特教/用书/筛选都不清空，加入时记录来源视图——攒了多少、来自哪个视图一目了然
- **下载前确认弹窗按视图分组**：清单明细按「学制·特教·用书」分组展示（如 ── 六三·教师用书 ──），核对与操作轨迹对得上
- **「导出 CSV」**：筛选结果一键导出 CSV 文件（含用书列，带 BOM，Excel 直接打开不乱码）
- **筛选「重置」按钮**：筛选行尾一键清空全部条件并复位用书视图，不必先唤出「已选条件」栏

### 修复

- **特殊教育 × 五四学制恒为空集，入口防呆**：平台 300 条特殊学校教材全部未标注学制，而「五四」按显式标注匹配——该组合永远 0 条结果。现由级联候选计数自动置灰不可用开关（特教亮 → 五四置灰；五四亮 → 特教置灰；特教下教师用书同理），点击弹提示解释原因而非毫无反应；已选中的开关永不置灰，保证随时能切换出来。未来平台补标数据后按钮自动恢复可用
- **「已加入 ✓」状态与实际清单脱节**：任务结束后自动清除已下载链接时，此前不回收结果表里的「已加入 ✓」标记，页面不刷新就无法重新加入。现以清单文本为单一真源随时对账，手动编辑、清空、找回链接等所有改动路径同步生效
- **「确认下载」连点重复提交**：确认弹窗点击后按钮立即禁用并显示「提交中…」，不再可能向服务重复发起任务

### 变更

- **移动端适配补齐**：窄屏下筛选下拉与关键词框撑满整行；目录列表取消「页面内滚动 + 表格内滚动」双层滚动改为随页滚动；chip 删除「×」、行内小按钮等触控目标放大到推荐尺寸；适配 iPhone 底部安全区
- **文案统一**：界面中文引号统一为「」；更新弹窗「手动更新」更名「下载安装包」（名实对齐，该按钮本就是用浏览器下载安装包）；学制按钮悬浮提示去掉间隔号，与界面文字一致
- **键盘可达性**：教材视图的学制/用书单选组支持 ←/→ 方向键切换（WAI-ARIA 惯例）

## [1.4.0] · 2026-09-09

「找对教材」版本：教材目录里混排的五·四学制变体与特殊学校教材从此各归其位；「一键更新」内置国内镜像加速。

### 新增

- **「教材视图」栏：六三/五四学制切换 + 特殊教育开关**：筛选区顶部新增独立视图栏。「六三学制」（默认选中）与「五四学制」为一组单选——六三视图为常规教材，自动排除五·四学制变体（348 条）与盲校/聋校/培智等特殊学校教材（300 条），普通用户的列表更干净；切到五四只看五·四学制教材。「特殊教育」是独立开关，点亮后只看特殊学校教材（含盲校/聋校/培智），可与任一学制组合。切换后学段/年级/科目等候选自动级联更新，选择保存在本地，下次打开自动恢复
- **「一键更新」内置国内镜像加速**：更新包下载默认走 ghfast.top 等前缀式镜像，逐个尝试、失败自动回退 GitHub 直连——无需配置即可显著提高国内下载成功率。`SHUDAOLE_UPDATE_MIRROR` 可指定自建镜像，`SHUDAOLE_NO_MIRROR=1` 禁用镜像始终直连。完整性校验不受影响：SHA256SUMS 始终优先从官方 Release 拉取

## [1.3.9] · 2026-09-09

「保存位置透明」版本：默认下载目录从「软件安装文件夹下的 downloads」改到用户「下载」目录，界面实时显示真实保存路径。

### 变更

- **默认保存目录改到用户「下载」目录**：新默认为 `C:\Users\<用户名>\Downloads\书到了`（macOS/Linux 为 `~/Downloads/书到了`）。原默认 `downloads` 是相对路径，按进程工作目录解析——从快捷方式、右键打开等不同方式启动时 CWD 不同，会出现「提示下载完成却找不到文件」；装在 Program Files 时更会因普通权限写不进直接失败。现与启动方式彻底解耦且必定可写。首次打开时保存目录输入框自动回填绝对路径；CLI 的 `-o/--output` 保持 `./downloads` 不变
- **保存目录实时提示**：目录输入框下方常驻显示解析后的真实绝对路径；手动修改输入框时实时跟随。目录不可写时红字警告并引导改用「浏览…」选择其他位置（此前要等下载失败才知道）

## [1.3.8] · 2026-09-09

「一键更新真一键」版本：自动更新下载完不再需要第二次点击；删除名不副实的「含课件资源」开关。

### 修复

- **自动更新「下载后不安装」**：原代码下载就绪后线程即停，用户需再点一次「立即安装」才真正安装；现点击「一键更新」= 下载 + 安装全自动。点击时一次确认「将下载并自动安装，安装时书到了会退出」，中间不再打断
- **Windows 安装器静默失败可诊断**：安装器从 `PrivilegesRequired=lowest` 改为 `admin`（UAC 取消时 CreateProcess 直接失败，前端如实报「UAC 未确认或权限不足」，而非先前静默吞掉）；去掉安装器参数的 `/SUPPRESSMSGBOXES`（吞错误框无痕），去掉 `/RESTARTAPPLICATIONS`（与 [Run] 启动项重复导致双开）

### 变更

- **按钮文案全局统一**：「查看新版」→「查看新版发布页」（横幅+弹窗一致）；「一键下载」→「手动更新」（用浏览器下载更新包）；「自动更新」→「一键更新」（程序内下载+安装）；弹窗新增「手动更新」按钮
- **静默更新后自动启动新版本**：安装器 [Run] 去掉 `skipifsilent`，与界面「安装完成后自动打开新版本」的承诺一致

### 移除

- **「含课件资源」筛选开关**：实测目录里 279 条「资源类」中 252 条是平台把教材按课时拆分的「第N课 xxx」（90% 无学段/学科标签），真正字面带"课件"的只有 9 条且是《教学指南和配套课件使用说明》这类说明文档，教学设计/教学案例 0 条——开关名不副实，且默认条件下会冒出 279 条无标签条目污染列表。CLI 参数 `--include-resources` 保留给高级用户

## [1.3.7] · 2026-09-09

「防错版」：围绕「家长下错教材版本」痛点的四层防线，筛选交互全面增强。

### 新增

- **下载前确认弹窗**：点「开始下载」先弹窗展示完整任务清单（书名/年级科目/版本/教师用书标识，最多 50 行 + 汇总），同年级同科目多版本并存时逐行 ⚠ 标注并汇总提示，确认后才真正下载——家长下错版本的最后防线
- **常用组合一键芯片**：筛选区顶部「初中·人教全套」「高中·北师大全套」等快捷按钮（按目录实际内容生成），一键选好学段+版本，再补年级或直接全部加入
- **版本识别提示**：版本下拉与结果列表悬浮显示出版社全称与封面识别特征（27 个常用版本），家长不知道孩子用哪版时可对照课本封面确认
- **多版本并存警示徽标**：未选版本时，结果中同年同学科存在多版本会在版本列显示「⚠ N版并存」，风险显性化
- **默认只看学生用书**：用途维度（audience）进入筛选下拉并默认选中「学生用书」，教师用书不再与学生教材混排（可手动切换查看）

### 变更

- 版本分组新增「北师大系列」（北师大版/华东师大版），下拉两级候选同步
- 版本识别提示文案按「出版社全称 + 封面特征」原则编写，华东师大版特别标注与北师大版的区分点

## [1.3.6] · 2026-09-09

「全学段开放」版本：检查更新弹窗化、令牌窗口自动关闭、小学到高中全学段开放。

### 新增

- **检查更新弹窗反馈**：点「检查更新」后以弹窗明确告知结果——已是最新版本时直接说明；发现新版本时展示「当前版本 → 最新版本（大小）」，弹窗内可直接「立即下载更新」（实时进度、SHA256 校验、Windows 静默安装）或跳转发布页
- **开放全部学段**：学段筛选开放小学 / 初中 / 高中 / 特殊教育全范围，全部年级可选（原先仅小学）
- **版本全量可见**：未分组版本标签（北师大版、外研版等）也按结果数列出版本下拉，不再隐藏；人教系列、沪教版保留"整套选"分组

### 变更

- **令牌获取成功后自动关闭浏览器窗口**：抓取完成改用 CDP 优雅关闭浏览器（兼容 Edge 启动加速等进程树脱离场景），失败时回退强制结束；用户放弃仍即时关闭，不再需要手动关窗

## [1.3.5] · 2026-09-09

「三端原生 + 自动更新」版本：macOS/Linux 原生安装包、应用内检查与自动更新、下载反误报缓解。

### 新增

- **macOS 原生安装包**：`ShudaoLe-X.Y.Z-macos.dmg`（内含真正的「书到了.app」，带独立图标、Dock 常驻、双击即启），替代原先的 zip 压缩包
- **Linux 原生安装包**：`ShudaoLe-X.Y.Z-linux-x64.deb`——Debian/Ubuntu 双击即装，自动登记应用菜单与系统图标（程序位于 /opt/shudaole）；tar.gz 继续保留供其他发行版使用
- **应用内自动更新**：更新横幅新增「自动更新」按钮——自动下载新版本更新包（实时进度、SHA256 完整性校验，校验不符自动删除重下），Windows 下载完成后一键静默安装（程序自动退出交由安装器接管）；macOS/Linux 下载完成后打开所在目录解压/挂载安装。失败时显示原因并可重试，不影响当前版本继续使用
- **手动检查更新**：界面右下角版本号旁新增「检查更新」按钮，随时手动查新版
- **发布完整性**：Release 附件新增 `SHA256SUMS.txt` 全包校验和，用户可核对下载是否被篡改
- **可选代码签名**：发布流水线支持配置代码签名证书（Secrets），配置后安装器自动签名，显著降低 SmartScreen 误报

### 文档

- README 新增「下载被浏览器拦截」处理指南（保留下载/绕过 SmartScreen/误报申诉/哈希核对）；三平台下载表更新为 dmg/deb 原生包

## [1.3.4] · 2026-09-09

「体检修复版」：全项目 bug + 安全审查后的问题清零批次，无新功能。

### 修复

- **更新横幅「查看新版 / 一键下载」按钮生效**：此前界面传参与服务端校验不匹配，按钮点击从未有过任何效果（P0）
- **并发下载同名竞态**：两本教材解析出相同文件名时可能同时写同一个临时文件导致 PDF 损坏；现改为锁内原子分配，同名自动挪 (2)、(3)（P0）
- **下载重定向安全**：下载/详情请求改为逐跳复检出站闸门，服务器 302 到内网地址不再被自动跟随（SSRF 加固）
- **令牌不再泄漏进日志/任务记录**：URL 参数方式下载失败时的错误文本会内嵌含令牌的地址，现已统一脱敏后才展示与落盘
- **快速双击「开始下载」不再可能双开任务**（检查与置位合并进同一临界区）
- **一键获取令牌成功后，令牌不再滞留服务内存状态**（仅随首次查询一次性交付）

### 变更

- **「记住令牌」勾选框真实生效**：勾选且页面填写了令牌时，令牌会写入 token.txt 供下次使用（此前在界面模式下为无效开关）
- 目录索引接口拒绝来自外部页面的跨站访问（img 标签触发目录重下的骚扰面消除）
- Windows 保留设备名（CON/NUL/COM1…）作书名时自动加前缀，不再下载必败
- 安装器：移除过时的卸载清理条目；卸载完成时询问是否一并删除配置目录（登录令牌/缓存）
- 打包配置隐藏导入修正为 shudaole.token；mypy 门禁放开到全包（消除 CI 类型检查盲区）

## [1.3.3] · 2026-09-09

「独立窗口版」：界面不再挤在浏览器标签页里。

### 变更

- **界面以独立窗口打开**：启动后自动用 Edge/Chrome 的应用模式（`--app`）打开界面——无地址栏、任务栏独立图标、自带窗口标题，观感即桌面应用
- 未装 Edge/Chrome（或启动失败）时自动回退系统默认浏览器，功能不受影响
- 从其他程序跳转界面（如重复双击 exe 复用已有实例）同样走独立窗口

## [1.3.2] · 2026-09-09

「体验修复版」：安装/卸载流程与目录选择对话框的用户可感知问题修复。

### 修复

- **升级安装不再卡「程序正在运行」**：安装/卸载前自动结束运行中的书到了（任务有持久化，无损；旧版本弹的「关闭程序」按钮对无窗口进程永远无效，已替换为自动处理）
- **目录选择不再闪黑窗**：选目录改在程序内直接调用系统现代对话框（资源管理器同款），不再出现命令行黑窗
- **目录选择在高缩放屏下清晰**：声明 DPI 感知，150%/200% 缩放不再发糊
- 目录选择 COM 调用异常时自动回退旧方案，功能不失效

## [1.3.1] · 2026-09-09

「内部优化版」：P3 三项落地，无功能变化，重刷界面与代码结构。

### 变更

- **前端轮询统一**：界面 4 个独立定时器合并为单心跳协调器，页面切后台自动降频（×4），状态轮询按运行状态自适应——后台 CPU 占用更低
- **GUI 路由表化**：本地服务的 11 个端点从 if-elif 改为字典分发，行为零变化，后续加端点更稳

### 文档

- 新建本 CHANGELOG；README 目录结构、下载方式、开发笔记与 v1.3.0 现状对齐

## [1.3.0] · 2026-09-09

「功能增强版」：P2 六项落地 + P1 工程化地基，首个提供 Windows 安装包的版本。

### 新增

- **并发下载**：多文件并行下载（默认 3 线程，界面可调 1–5），单文件失败不影响整批，随时可取消
- **任务恢复**：关闭窗口后未完成任务自动保存，下次启动一键续传（含 `.part` 断点续传强化）
- **CLI 脚本化**：`--export` 导出 CSV（带 BOM）/Markdown 书单、`--dry-run` 仅列出待下载项、退出码规范化
- **更新检查**：启动时静默比对 GitHub Releases 最新版本（仅版本号，无遥测），界面一键跳转下载；`SHUDAOLE_NO_UPDATE_CHECK` 可关闭，`SHUDAOLE_UPDATE_MIRROR` 可配镜像
- **Windows 安装包**：Inno Setup 简体中文向导（开始菜单/桌面快捷方式、标准卸载器、可选创建下载子目录），与绿色版 zip 并存

### 变更

- **出版社配置外置**：`~/.config/shudaole/pubs.json` 深合并扩展出版社分组，无需改代码
- 界面轮询合并为统一刷新协调器（单心跳，按页面可见性与任务状态调节频率），降低常驻开销
- GUI 服务端路由改为字典分发（新增端点 = 一个方法 + 一行表项）

### 工程（不影响使用方式）

- 单文件拆分为 `shudaole/` 包（13 模块 + gui 子包），根脚本保留为兼容入口
- pytest 测试 59 例 + 覆盖率门禁；CI 矩阵 Python 3.8/3.10/3.12（ruff + mypy + pytest）
- 统一 logging（文件 + 控制台/界面双通道）；保持 Python 3.8 兼容基线

## [1.2.1] · 2026-09-09

「正确性修复」补丁版（P0 五项）。

### 修复

- 浏览器 profile 目录命名泄漏（用户名路径外泄至临时目录名）
- 目录缓存非原子写（中断产生损坏缓存导致目录加载失败）
- WebSocket 连续帧支持（部分网络环境令牌抓取收不到完整响应）
- 令牌落盘位置与权限（迁移到用户配置目录，收紧文件权限）
- 收窄裸 `except`（异常不再静默吞掉，便于定位问题）

## [1.2.0] · 2026-09-05

「跨平台支持」版本。

### 新增

- macOS / Linux 构建：CI 三平台自动打包发布（macOS 未签名，首跑需绕过 Gatekeeper）
- 一键获取令牌支持三平台（自动探测系统 Edge/Chrome，无需装 Python）

### 修复

- 中文名环境多实例端口冲突（表现为「点了下载没反应」）

## [1.1.1] · 2026-09-05

本地服务安全加固。

### 安全

- Host 头校验：拦截 DNS 重绑定攻击（恶意网页将域名解析到 127.0.0.1 窃取令牌/任务数据）
- POST 请求 Origin 校验：拦截浏览器跨站伪造请求（CSRF）
- 修复「选择目录」对话框的 PowerShell 单引号注入
- 「打开文件/目录」仅允许作用于当前任务下载目录内

## [1.1.0] · 2026-09-05

界面体验修复（toast 不遮挡操作、取消粘性表头、已选条件空灰条、窄窗口换行等）。

## [1.0.2] · 2026-09-04

版本号单一数据源：界面显示与 exe 文件属性统一从 `version_info.txt` 解析，发版只改一处。

## [1.0.1] · 2026-09-04

界面品牌化重构：印章 logo、朱砂主色、步骤化布局（功能逻辑不变）。

## [1.0.0] · 2026-09-04

首个公开版本：一键下载国家中小学智慧教育平台官方教材 PDF（Windows 绿色版）。

[1.3.3]: https://github.com/tau625/ShudaoLe/releases/tag/v1.3.3
[1.3.2]: https://github.com/tau625/ShudaoLe/releases/tag/v1.3.2
[1.3.1]: https://github.com/tau625/ShudaoLe/releases/tag/v1.3.1
[1.3.0]: https://github.com/tau625/ShudaoLe/releases/tag/v1.3.0
[1.2.1]: https://github.com/tau625/ShudaoLe/releases/tag/v1.2.1
[1.2.0]: https://github.com/tau625/ShudaoLe/releases/tag/v1.2.0
[1.1.1]: https://github.com/tau625/ShudaoLe/releases/tag/v1.1.1
[1.1.0]: https://github.com/tau625/ShudaoLe/releases/tag/v1.1.0
[1.0.2]: https://github.com/tau625/ShudaoLe/releases/tag/v1.0.2
[1.0.1]: https://github.com/tau625/ShudaoLe/releases/tag/v1.0.1
[1.0.0]: https://github.com/tau625/ShudaoLe/releases/tag/v1.0.0
